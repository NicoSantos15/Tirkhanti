@model IEnumerable<Tirkhanti_R12.Models.Emotions>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentEmotion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentEmotion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmotionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmotionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmotionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
